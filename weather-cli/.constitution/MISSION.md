# Mission

Build a CLI tool that fetches and displays current weather for a given city using a public API.
Written in Rust. Single binary. No external API key required (use wttr.in or similar free service).
